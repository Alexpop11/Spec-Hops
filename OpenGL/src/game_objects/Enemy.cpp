#include "Enemy.h"

Enemy::Enemy(const std::string& name, float x, float y)
    : Character(name, x, y) {}

void Enemy::render(Renderer& renderer) {
    // Rendering logic for Enemy
}

void Enemy::update() {
    // Update logic for Enemy
}
